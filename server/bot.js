class Bot{
  constructor(){
    // sets cookies, proxies, various navigation options etc for later usage
  }
  init(){
    // create a browser instance
    // create a page
    // setup all useragent, proxy etc required
  }
  navigate(){
    // loads a page
  }
  fillForm(){
    // waits for element
    // fill the element with required data
  }
  clickBtn(){
    // clicks specific button, simulated/triggers a mousedown/keydown/click event
  }
  getText(){
    // gets text for certain selector on the page
  }
  getTitle(){
    // get current page title
  }
  getCookies(){
    // get current page cookies
  }
}

module.exports = Bot;